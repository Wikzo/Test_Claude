# Add project specific ProGuard rules here.
# Minification is disabled for this build (isMinifyEnabled = false), so these
# rules are currently inert placeholders for when release shrinking is enabled.
